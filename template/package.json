{
  "name": "foobar",
  "version": "1.0.0",
  "main": "foobar.sketchplugin",
  "license": "MIT",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "foobar",
    "manifest": "src/manifest.json"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "skpm-builder": "^0.1.0"
  }
}
